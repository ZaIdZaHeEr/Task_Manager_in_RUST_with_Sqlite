[package]
name = "personal_expense_pro"
version = "0.1.0"
edition = "2024"

[dependencies]
sqlx = { version = "0.7", features = [
    "sqlite",
    "runtime-tokio-rustls",
    "macros",
] }
tokio = { version = "1", features = ["full"] }
